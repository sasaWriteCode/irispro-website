# Project Rules

## Verification
- After coding changes, do NOT use the browser subagent to preview or verify visually.
- The user will check the UI manually in their own browser.
- Only run `npm run build` to verify that changes compile without errors.
